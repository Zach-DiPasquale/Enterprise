@page
@model KennUwareHR.Pages_Leave.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Leave</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Leave.StartTime" class="control-label"></label>
                <input asp-for="Leave.StartTime" class="form-control" />
                <span asp-validation-for="Leave.StartTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Leave.EndTime" class="control-label"></label>
                <input asp-for="Leave.EndTime" class="form-control" />
                <span asp-validation-for="Leave.EndTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Leave.Employee" class="control-label"></label>
                <select asp-for="Leave.EmployeeId" class="form-control" asp-items="@Model.EmployeeSL">
                    <option value="">-- Select Employee --</option>
                </select>
                <span asp-validation-for="Leave.Employee" class="text-danger" />
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
